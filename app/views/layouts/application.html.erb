<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Bitstop App Site" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Bitstop App Site" %>">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
    <script src="https://use.fontawesome.com/5e9aa853e8.js"></script>
  </head>
  <body class="landing-page landing-page1">
    <%= render 'layouts/messages' %>
    <%= yield %>
  </body>
</html>
